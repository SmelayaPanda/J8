package smel.pan.Pack2;import java.util.function.Supplier;/** * Created by SmelayaPanda on 20.04.2016. * Suppliers produce a result of a given generic type. * Unlike Functions, Suppliers don't accept arguments. */public class SupplierTest {}class Person {    String name;    int age;    Supplier<Person> personSupplier = Person::new;    public Person() {    }    public Person(String name, int age) {        this.name = name;        this.age = age;    }    //Trouble!    // Person person = personSupplier.get(); //new Person (with empty base constructor!)    // And their constructor make StackOverflowError =)    // And, I still haven't made up for not base constructor =(}