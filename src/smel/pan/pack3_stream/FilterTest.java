package smel.pan.pack3_stream;import java.util.ArrayList;import java.util.List;/** * Created by SmelayaPanda on 21.04.2016. * * @FilterSteram Filter accepts a predicate to filter all elements of the stream. * This operation is intermediate which enables us to call another stream operation (forEach) on the result. * ForEach accepts a consumer to be executed for each element in the filtered stream. * ForEach is a terminal operation. * It's void, so we cannot call another stream operation. */public class FilterTest {    public static void main(String[] args) {        List<String> strList = new ArrayList<>();        strList.add("Apple");        strList.add("Mac");        strList.add("IBM");        strList.add("Microsoft");        strList.add("Oracle");        strList.add("Sun");        strList                .stream()                .filter(s -> s.startsWith("M"))                .forEach(System.out::println);  //Mac, Microsoft    }}