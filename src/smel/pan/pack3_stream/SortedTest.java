package smel.pan.pack3_stream;import java.util.ArrayList;import java.util.List;/** * Created by SmelayaPanda on 21.04.2016. * * @SortedStream Sorted is an intermediate operation which returns a sorted view of the stream. * The elements are sorted in natural order unless you pass a custom Comparator. */public class SortedTest {    public static void main(String[] args) {        List<String> strList = new ArrayList<>();        strList.add("Apple");        strList.add("Mac");        strList.add("IBM");        strList.add("Microsoft");        strList.add("Oracle");        strList.add("Sun");        strList                .stream()                .sorted()                .filter(s -> s.contains("M"))                .forEach(System.out::println); //IBM, Mac, Microsoft        /**         * Keep in mind that sorted does only create a sorted view of the stream         * without manipulating the ordering of the backed collection.         * */    }}